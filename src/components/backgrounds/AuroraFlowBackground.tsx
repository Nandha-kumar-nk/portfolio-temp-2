import React from 'react';
import { GalaxyBackground, GalaxyBackgroundProps } from './GalaxyBackground';

export const AuroraFlowBackground: React.FC<GalaxyBackgroundProps> = (props) => {
  return <GalaxyBackground {...props} />;
};

export default AuroraFlowBackground;
