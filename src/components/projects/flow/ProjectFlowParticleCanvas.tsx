import React from 'react';

interface ProjectFlowParticleCanvasProps {
  activeProjectId?: string;
  isTransitioning?: boolean;
  direction?: 'forward' | 'backward';
}

export const ProjectFlowParticleCanvas: React.FC<ProjectFlowParticleCanvasProps> = () => {
  // Integrated into Global Galaxy Particle System
  return null;
};

export default ProjectFlowParticleCanvas;
